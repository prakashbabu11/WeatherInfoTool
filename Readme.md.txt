Here are the steps you can include in the README.md file to run the application:

Weather Info Tool

This command line tool allows you to fetch weather information for a specific city.

Prerequisites

.NET Core SDK (version X.X.X or higher) installed on your machine
Getting Started
Clone the repository:


Copy code :- 
git clone <repository_url>
Navigate to the project directory:

bash
Copy code
cd WeatherInfoTool
Build the application:

bash
Copy code
dotnet build
Run the application:

bash
Copy code
dotnet run
Enter the name of the city for which you want to fetch weather information.

The application will display the temperature and wind speed for the specified city.

Running Tests
To run the tests for this application, follow these steps:

Navigate to the test project directory:

bash
Copy code
cd WeatherInfoTool.Tests
Run the tests:

bash
Copy code
dotnet test
This command will execute all the tests in the project and display the test results.

That's it! You have successfully run the Weather Info Tool and its tests.